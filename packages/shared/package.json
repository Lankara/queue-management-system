{
  "name": "@queue-management/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared enums, constants, and TypeScript types for the Queue Management System.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit --pretty false",
    "typecheck": "tsc --noEmit --pretty false",
    "test": "node -e \"console.log('No shared package tests configured yet')\"",
    "clean": "node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true });\""
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}