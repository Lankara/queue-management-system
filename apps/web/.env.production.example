# Web production environment template.
# NEXT_PUBLIC_* values are browser-visible. Never put secrets here.
# Rebuild the web container after changing these values because they are baked into Next.js.

NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com/api
NEXT_PUBLIC_PUBLIC_APP_URL=https://yourdomain.com