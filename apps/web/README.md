# Queue Management Web

Next.js admin and operator web app foundation for the Queue Management System.

## Development

```powershell
pnpm --filter @queue-management/web dev
```

Set `NEXT_PUBLIC_API_BASE_URL` in `.env.local` if the API is not running at `http://localhost:4000/api`.
